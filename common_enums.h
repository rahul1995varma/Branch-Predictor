#include<iostream>

using namespace std;


enum {
  STRONGLY_NOT_TAKEN = 0,
  WEAKLY_NOT_TAKEN = 1,
  WEAKLY_TAKEN = 2,
  STRONGLY_TAKEN = 3,
};

enum {
  BIMODAL = 0,
  GSHARE = 1,
  HYBRID = 2,
};

enum {
  NOTTAKEN = 0,
  TAKEN = 1, 
};
